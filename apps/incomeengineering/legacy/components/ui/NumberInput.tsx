"use client";

/** Thin alias — canonical implementation lives in `@cb/ui` for reuse across model apps. */
export { FormattedNumberInput as NumberInput } from "@cb/ui";
export type { FormattedNumberInputProps as NumberInputProps } from "@cb/ui";
